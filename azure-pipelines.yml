trigger:
- main

pool:
  name: PoolProjet2   # <--- Utilise ton pool agent personnel ici

steps:
# 1. Checkout du code
- checkout: self

# 2. Installation des dépendances (exemple Node.js, adapte si besoin)
- script: |
    echo "Installation des dépendances"
    npm install
  displayName: "Install dependencies"

# 3. Build du projet (exemple Node.js)
- script: |
    echo "Build du projet"
    npm run build
  displayName: "Build project"

# 4. Exécution des tests (exemple Node.js)
- script: |
    echo "Tests"
    npm test
  displayName: "Run tests"

# 5. Déploiement simple (exemple, adapte à ton besoin)
- script: |
    echo "Déploiement en cours..."
    # Ajoute ici tes commandes de déploiement
  displayName: "Deploy project"
